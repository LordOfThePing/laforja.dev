# TODO

Tareas pendientes y en curso. Ver `CLAUDE.md` (§2) para el workflow.

## En curso

- **Bootstrap del backend `apps/api`** — en curso por @claude —
  Hono + Bun + Drizzle. Schema v1 completo (users, tools, categories,
  unlocks, subscription_events), endpoints públicos `GET /health`,
  `GET /api/tools`, `GET /api/tools/:slug`, seed con las 8 herramientas
  del mock. Sin auth ni pagos todavía (van en tareas separadas).

## Pendiente

_(vacío — se irá poblando a medida que avance el proyecto)_
